/** 首次进入：较长仪式感，与 App 定时器一致 */
export const ENTRY_SPLASH_FIRST_MS = 5200;

/** 再次进入：短促衔接 */
export const ENTRY_SPLASH_RETURN_MS = 2200;

/** @deprecated 请用 FIRST / RETURN，由 `isFirstEntrySplash()` 选用 */
export const ENTRY_SPLASH_AUTO_MS = ENTRY_SPLASH_FIRST_MS;
